import HeaderNav from "./header-nav";
const Header = () => {
  return (
    <header className={"bg-grey-80 top-0 py-4 sticky w-full z-50"}>
      <HeaderNav />
    </header>
  );
};

export default Header;
